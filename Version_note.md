# Approval System 6.0.0 Release Notes

發布日期：2026-08-10  
比較基準：上一個 Git 提交 `012d29a`（新增薪水計算，修改匯率頁面）

## 版本摘要

6.0.0 著重於薪資結構與 Excel 薪資單、人員管理、簽呈操作、權限與安全性、排班／排休，以及 Docker 部署流程。這個版本也補強了測試、健康檢查、稽核紀錄與多語系介面。

## 主要新增功能

### 薪資結構與薪資試算

- 新增完整的員工薪資結構維護頁面。
- 支援月薪與時薪兩種薪資型態。
- 支援平日與假日時薪。
- 支援底薪、專業加給、職務加給、業務加給、加班加給、夜班加給及特休加給。
- 新增專業加給項目管理，以及員工與專業加給項目的對應關係。
- 新增每日時薪覆寫、薪資規則版本、假日資料及薪資試算結果保存。
- 支援薪資送審及核准流程。
- 薪資明細與總計共用同一次 BioLife 打卡資料快照，避免重複 API 呼叫及前後結果不一致。
- 修正月薪制固定薪資與各項加給被重複計算的問題。

### Excel 薪資單

- 新增薪資單 Excel 匯出功能。
- 加入 `openpyxl` 正式相依套件。
- Excel 顯示底薪及所有固定加給項目。
- 薪資摘要限制在 A～H 欄，超過八個項目時自動排列到第二組列。
- 「實發」固定於薪資區塊右下角，沿用相同格式並以粗體顯示。
- 每位員工的日明細與薪資總計使用相同的打卡資料快照。

### 人員管理

- 新增關鍵字、部門、角色及帳號狀態篩選。
- 預設只顯示啟用中的帳號。
- 每頁預設顯示 100 筆，可切換為 50 或 100 筆。
- 新增單筆停用功能及警告確認視窗。
- 新增批次處理模式、全選 checkbox 與批次停用功能。
- 禁止停用目前登入的帳號。
- 系統至少保留一位啟用中的系統管理員。
- 移除帳號永久刪除及批次刪除功能，保留歷史簽呈、簽核與薪資資料的帳號連結。
- 停用帳號無法再次登入。
- 批次帳號操作會寫入 audit log。

### 排班與排休

- 新增排休申請及主管管理介面。
- 排休日期只允許選擇下一個月份，並正確處理跨年份情況。
- 新增排班最低人數規則與排休衝突提示。
- 強化跨日班別及夜班打卡資料的分組與顯示。

### 系統管理與權限

- 新增角色權限管理介面及資料庫權限覆寫。
- 新增系統設定入口與稽核紀錄頁面。
- 增加薪資試算、薪資規則、送審、核准及人資功能權限。
- 強化管理頁、API 與 JSON request 的權限及輸入檢查。

## 簽呈功能改善

- 簽呈改為依序簽核，只有目前順位的簽核人可以核准或退回。
- 新增退回原因保存與顯示。
- 改善簽呈列表的標題、類型及建立者複合篩選。
- 新增建立者候選清單及分頁處理。
- 強化簽呈建立 transaction，避免文件建立成功但簽核人寫入失敗。
- 改善簽呈狀態通知及錯誤處理。

## 安全性與穩定性修正

- 移除登入流程中手動覆寫 Flask session cookie 的錯誤行為。
- 新增全域 CSRF 驗證及 AJAX CSRF header 支援。
- 強化 production 環境的 `SECRET_KEY` 驗證。
- 加入 request ID、結構化日誌及統一錯誤頁面。
- 新增 `/healthz` 健康檢查端點。
- 改善資料庫 connection、transaction、commit 與 rollback 處理。
- 新人建立流程加入 BioLife 建立失敗時的補償清理。
- 修正停用帳號仍可重新登入的問題。

## UI 與多語系

- 更新首頁、導覽列、Topbar、Footer、登入頁及多個功能頁面樣式。
- 改善手機與小螢幕版面。
- 新增繁體中文、英文及日文介面文字。
- 人員管理的停用、批次處理與確認視窗支援多語系。
- 修正頁面自訂 JavaScript block 未載入，導致停用及批次按鈕沒有反應的問題。
- DataTables 外掛改為選用；外掛不存在時不再阻止其他按鈕事件。

## 資料庫與 Migration

- 新增 migration runner。
- 新增／調整排班、排休、薪資規則、薪資結構、專業加給、員工加給對應、假日及 audit log schema。
- 增加必要索引與缺少欄位的相容 migration。
- Migration 採可重複執行設計，降低既有環境升級風險。

## Docker 與部署

- Docker 改用 Gunicorn 啟動 Flask 應用程式。
- 新增容器 HEALTHCHECK。
- `.dockerignore` 排除 `.env`、Git、本機虛擬環境、cache 與 log，避免秘密資料被打包進 image。
- 新增 Docker 重建及健康檢查腳本。
- 新增部署與維運 runbook。

## 測試狀態

6.0.0 發布前完整測試結果：

```text
60 passed, 1 skipped
```

測試範圍包含：

- 登入、session cookie 與 CSRF。
- 人員搜尋、篩選、停用及批次操作。
- 簽呈篩選、權限及依序簽核。
- 排休日期限制。
- 薪資規則、薪資結構與各項加給。
- BioLife 打卡資料與跨日班別。
- 薪資 Excel 排版及單次資料抓取。
- Migration、health check 及系統設定頁面。

## 升級注意事項

1. 部署前請備份 MySQL 資料庫。
2. 執行 migration：

   ```bash
   python migrate_db.py
   ```

3. 重新安裝相依套件，確保包含 `openpyxl`：

   ```bash
   pip install -r requirements.txt
   ```

4. Docker 環境必須重新 build，單純 restart 舊容器不會安裝新套件：

   ```bash
   docker build -t approvalsys:latest .
   ```

5. `.env` 不會再被包入 image，啟動容器時需以 `--env-file .env` 或其他秘密管理方式注入設定。
6. 停用帳號仍保留於資料庫，以維持歷史簽呈、簽核與薪資資料關聯。

## 已知事項與後續工作

- Python 3.12 下仍有 Flask、Werkzeug、pytz 與部分測試工具的 deprecation warning，後續需安排相依套件升級。
- 遲到／早退薪資判斷目前依產品決策維持關閉，待正式排班資料啟用後再開啟。
- 薪資 Excel 匯出 loading UI 已列入 `todolist.md`，尚未包含在 6.0.0。
- 首頁版本測試仍需改為與 `APP_VERSION` 共用單一版本來源。

