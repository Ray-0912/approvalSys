# Approval System 待辦清單

本待辦清單僅保留尚未完成項目，依風險與實作效益排序。

## P4 - 測試與發布品質

1. 建立 CI 流程
- 補上 Pull Request 自動執行 lint、測試與安全檢查。
- 建立關鍵檢查未通過即禁止合併的流程。

2. 建立測試 fixture 與種子資料
- 提供本機與 CI 可重現的資料庫樣本狀態。

3. 補上負載與可靠性檢查
- 對排班儲存與簽核動作進行基本並發測試。

## P5 - 部署與維運

1. 生產流程模型調整
- 將排程改為獨立 worker 行程執行（不與 Flask Web 行程同跑）。
- 以 WSGI 伺服器（`gunicorn`）與可控 worker 數量啟動。

2. 容器硬化
- 容器使用非 root 帳號執行。
- 加入 healthcheck 與啟動前相依性檢查。
- 確保 `.dockerignore` 排除虛擬環境與輸出資料夾。

3. 環境範本與啟動檢查
- 提供 `.env.example` 並清楚註記變數用途。
- 啟動時檢查必要環境變數是否缺漏。

4. 備份與災難復原 Runbook
- 定義資料庫備份頻率與還原演練流程。