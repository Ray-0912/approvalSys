# Approval System 待辦清單

## 專案 Review 待處理項目（2026-08-10）

- [ ] 薪資 Excel 匯出時顯示 loading UI，並在下載開始或發生錯誤後恢復按鈕狀態。

### P0－權限與資料安全

- [ ] 修正簽呈編輯的 IDOR：`POST /p/edit/<doc_id>` 必須重新確認目前使用者是建立者，且文件仍為可編輯的待簽狀態；資料庫 `UPDATE` 也要加入 `creator`、`status` 條件並檢查 `rowcount`。
- [ ] 修正簽呈刪除授權：一般使用者只能撤回自己尚未完成的簽呈；管理員刪除應使用獨立權限，例如 `p_delete_admin`。
- [ ] 將簽呈刪除改為 soft delete，或確認 `document` 與 `doc_approval_record` 已有正確的 foreign key cascade，避免留下孤兒紀錄。
- [ ] 補上文件層級的檢視授權：只有建立者、簽核人、通知對象或具管理權限者可以查看簽呈內容，避免 `p_view` 使用者讀取所有人的文件。
- [ ] 移除員工同步共用密碼 `zxcvbnma`：改用 `secrets` 產生一次性密碼或設定密碼連結，並要求首次登入修改密碼。
- [ ] 登入狀態每次 request 應重新確認帳號存在且 `activation = 1`；角色或 team 異動後也要讓舊 session 權限失效。

### P1－簽核資料一致性

- [ ] 將核准、駁回與撤回的「簽核紀錄更新＋文件狀態更新」合併成單一資料庫 transaction。
- [ ] 簽核操作使用 `SELECT ... FOR UPDATE` 或等效的原子條件更新，避免同時核准／駁回造成競態。
- [ ] 所有狀態更新檢查受影響列數，只有 transaction 成功後才寄通知信，並防止重複點擊寄出重複通知。

### P2－測試與相依套件維護

- [ ] 首頁版本測試目前仍硬編碼版本字串；後續改為與 `APP_VERSION` 共用單一來源，避免每次升版需同步修改測試。
- [ ] 補上簽呈跨帳號修改、刪除及檢視的授權測試。
- [ ] 補上簽核併發、transaction rollback、停用帳號與角色異動後舊 session 失效的測試。
- [ ] 規劃升級 Flask、Werkzeug 與相關相依套件；目前在 Python 3.12 執行測試會產生大量 deprecation warning。

### 已確認不列為缺陷

- 排班頁設計為所有已登入使用者皆可查看，因此不增加部門或角色限制。
- 遲到／早退判斷目前是刻意關閉，待排班功能正式啟用後再開啟。

---

本待辦清單僅保留尚未完成項目，依風險與實作效益排序。

## P4 - 測試與發布品質

已完成：
- 建立 CI 流程（GitHub Actions，執行測試）
- 建立測試 fixture 與可重用測試 client
- 補上基礎健康檢查與容器檢查入口

## P5 - 部署與維運

待辦：
- 將排程改為獨立 worker 行程執行（不與 Flask Web 行程同跑）
- 進一步強化容器安全性（非 root、啟動前相依性檢查）
- 進一步補齊環境變數檢查與部署文件
- 加入容器 healthcheck
- 提供基礎 runbook 與備份/還原流程文件
